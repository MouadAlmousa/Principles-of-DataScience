For assignment one :

    question one : You need to install tabulate library using this command "pip install tabulate" OR this one if you're using Anaconda "conda install tabulate"
    
    question two : You need to install seaborn library using this command "pip install seaborn" OR this one if you're using Anaconda "conda install seaborn"
                                    & FPDF library library using this command "pip install fpdf" OR this one if you're using Anaconda "conda install fpdf"
